{
  "name": "teleios-architecture",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "nodemailer": "^6.9.3"
  },
  "engines": {
    "node": ">=16"
  }
}
